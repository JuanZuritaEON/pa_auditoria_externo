import appReducer, { SAVE_APP_FLUX, SAVE_ERRORS } from './appContext'
import { apiSlice, S3Slice } from './appData.slice'

export {
  apiSlice,
  S3Slice,
  appReducer,
  SAVE_APP_FLUX,
  SAVE_ERRORS
}